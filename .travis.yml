jobs:
  include:

  - stage: deploy
    language: node_js
    node_js: node
    cache: npm
    before_script:
      - npm install
    script: 
      - npm run build
      - npm run test
    deploy:
      provider: npm
      email: $EMAIL
      api_key: $NPM_API_KEY
      on: release